language: android
sudo: false
jdk: oraclejdk8
android:
  components:
  - tools
  - android-25
  - build-tools-25.0.2
  - platform-tools
  - extra-android-m2repository
before_script:
- chmod +x gradlew
- openssl aes-256-cbc -K $encrypted_342b957251a9_key -iv $encrypted_342b957251a9_iv -in keystore.jks.enc -out keystore.jks -d || true
script:
- "./gradlew build"
after_failure: "cat /home/travis/build/czekoladowyPaczek/ZLog/zlog/build/outputs/lint-results-debug.html"
